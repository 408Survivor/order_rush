# 爆单时刻独立游戏开发 — 对话提炼稿

> 来源：旧会话「爆单时刻独立游戏开发」（2026-07-07 深夜 ~ 2026-07-08 凌晨，北京时间）
> 原始记录：`conv-33702b820a5408365b3ba5ae/agents/main/wire.jsonl`（1.7MB，34 轮，完整对话）；任务指定的 `conv-05da78c4044095a77b864d4c/agents/main/wire.jsonl`（154KB）实际只含该对话的第 0 轮（skill 推荐轮），两者开场白相同。
> 当时项目根目录：`/Users/liutongqing/Downloads/跨设备同步/游戏/爆单时刻/order_rush/`（现已迁移至新路径）
> 标注约定：【用户】= 用户决定/要求；【AI】= AI 建议/产出。

---

## 1. 开发规范

### 1.1 目录结构【用户提出，AI 落实】
- Godot 推荐结构：`res://scenes/`（.tscn）、`res://scripts/`（.gd 按功能子文件夹）、`res://assets/art|audio|fonts`、`res://ui/`、`res://autoload/`（全局单例如 OrderManager）。
- **文件存放铁律**：`.tscn`→`scenes/`、`.gd`→`scripts/`、`.png`→`assets/art/`、**根目录禁止堆放文件**。
- 实际建立的目录：
  ```
  order_rush/
  ├── assets/art/{characters,items,environment,ui,effects}/
  ├── assets/audio/{bgm,sfx,ambience}/
  ├── assets/fonts/
  ├── archive/generations/NNN-简要描述/
  ├── docs/{gdd,prompts}/
  ├── scenes/ | scripts/ | resources/   （预留）
  ├── README.md | ReadmeForAgent.md | Lookme.html
  └── .gitignore
  ```

### 1.2 命名规范【用户】
- 场景文件：PascalCase（`Microwave.tscn`、`MainScene.tscn`）
- 脚本与变量：snake_case（`microwave.gd`、`order_manager.gd`）
- 信号名：过去分词或动宾结构（`order_completed`、`item_picked_up`）
- 常量：UPPER_SNAKE_CASE

### 1.3 代码注释与文档【用户】
- 每个脚本顶部注释说明文件职责；每个函数注释输入/输出/副作用；复杂算法（优先级排序、时间裕度计算）逐行注释。
- **禁止魔法数字**：所有数值（加热时间 3 秒、耐心值 30 秒）必须定义为常量或 `@export` 导出变量。

### 1.4 版本控制【用户要求，AI 细化】
- 每个 Phase 完成 = 一个可提交里程碑；Phase 结束打 tag（如 `git tag p1`）。
- Commit message 用 Conventional Commits 简化版：`feat/fix/asset/docs/chore`。
- 每个 commit 必须可运行；定期推送 GitHub/Gitee 备份。
- P0 目标 commit：`p0: art style baseline`；P1 目标 tag：`p1`。

### 1.5 避免过度工程【用户】
- 第一阶段只实现一个可运行核心循环，禁止提前做多关卡、多存档、网络同步。
- 用 Godot 内置 Timer / Tween / AnimationPlayer，不造轮子；状态机用 match/枚举，不引入插件。
- 新想法先记到 `docs/ideas.md`，不急着实现【AI 补充】。

### 1.6 资源管理【用户】
- AI 生成图片导入 Godot 后统一设 **Filter: Nearest**（角色 Size Limit: 512【AI】）。
- 音频按类型分文件夹，用 AudioBus 管理 BGM/音效；定期清理未使用资源。

### 1.7 调试友好【用户】
- 保留 Debug UI 面板（F1 切换），实时显示 OrderManager 状态、活跃订单、设备占用。
- 用 `print_rich` / `print_debug` 输出关键状态变化。

### 1.8 AI 素材存档规范【用户设计结构，AI 补充模板】
- 结构：`archive/generations/NNN-简要描述/`，内含 `NNN.txt`（记录平台、参数设置、提示词、生成结果、经验记录）+ 当批全部图片（包括被淘汰的）。
- 选中图命名加标记：`xxx_v1(selected).png`【用户改的命名】。
- **复制到 `assets/art/` 最终目录时重命名为规范文件名，不带 `v`、`(selected)` 等版本字样**【用户明确要求】。
- `assets/art/` 只放最终入选素材；`archive/` 保留全部原始生成记录，两者都纳入 Git。

### 1.9 会话交接工作流【用户提出，AI 写入 `docs/工作流规范.md`】
- 开工语：「开始今天的开发，请阅读 ReadmeForAgent.md。」→ AI 读 ReadmeForAgent.md + docs/进度看板.md + 检查 assets/art/，按"待办事项（Next）"推进。
- 收工语：「同步到文件，我要休息了。」或「收工」→ AI 更新进度看板（百分比/活动记录/待办）、ReadmeForAgent.md（阶段/素材/Next/关键决策/日期）、给一句话总结。
- 其他话术：中断「我要中断，先不同步。」；紧急求助「紧急求助：XXX报错了。」（跳过加载直接解决）；回顾「回顾一下项目当前状态。」

### 1.10 文档体系分工【AI 建议 + 用户提出关键两件】
- `README.md`：给人看的项目总览【AI】。
- `ReadmeForAgent.md`：给 AI 的快速上下文，新会话零成本加载【用户提出】。
- `Lookme.html`：可视化进度看板（含已选素材展示），风格对齐用户个人网页 demo（Tailwind + Lucide + 玻璃拟态 + 渐变文字 + 暗黑模式切换），后续会与个人网页合并【用户提出】。
- `docs/开发手册_v1.0.md`、`docs/进度汇报模板.md`（4 种场景模板：Commit 完成/Phase 验收/Bug 求助/每日简报）、`docs/进度看板.md`、`docs/Phase_0_执行指南.md`、`docs/prompts/AI美术提示词库_v1.0.md`【均为 AI 产出】。

---

## 2. 设计决策（玩法/数值/经济/UI）

> 以下来自用户开场简报（前期调研成果），对话中原样确认，全部为【用户】决定；里程碑拆解为【AI】建议。

### 2.1 游戏定位
- 中文名「爆单时刻」，英文名 Order Rush。2D 俯视角预制菜小餐馆经营模拟，融合《飞机大厨》（高频操作爽感）+《杯杯倒满》（经营深度、PC 键盘操作）。堂食+外卖双线程；核心乐趣 = 时间管理 + 订单调度 + 空间移动。

### 2.2 三级出餐体系
- L1（即热）：冰柜取料理包→撕开→微波炉→装盘/装袋，约 6 秒
- L2（即烹）：多料包组合→炒锅按顺序现做，约 14 秒
- L3（现做+备菜）：开店前备餐模式做凉菜（拍黄瓜），营业中现做高价菜（小炒牛肉），约 25 秒

### 2.3 外卖调度（核心策略层）
- 订单到达时间 ≠ 骑手到达时间；每单显示"时间裕度"（骑手 ETA − 制作时间），红黄绿蓝四色紧急度提示；复杂菜提前做、简单菜延后。

### 2.4 经营与肉鸽
- 单局 7–10 分钟模拟一天营业。
- 每单收入 = 菜品基础价 + 打包费 + 平台补贴 − 平台扣点(基础 20%) + 好评奖励 − 差评罚款。
- 每日成本 = 食材成本(按售出量) + 固定房租 + 水电燃气(按设备使用次数) + 耗材费。
- 口碑值打烊后抽卡构筑。卡牌流派：走量流（满减单价-15%单量+30%、冲单每10单+50金）、品质流（L3售价+40%、打包费翻倍）、外卖专精（扣点-8%、超时罚减半）、堂食专精（熟客 20% 免看菜单）、成本流（食材-15%、水电-30%、耗材-20%）。

### 2.5 局外成长
- 人物 5–6 个：快手主厨(制作-15%)、打包达人(打包减半)、精明老板(扣点-5%)、现炒高手(L3售价+20%)、外卖老手(骑手容忍+5秒)、社交达人(小费+100%)。
- 设备升级：微波炉功率/双腔、冰柜容量、炒锅火力、自动封盖机、外卖打印机、保温柜、排烟系统。

### 2.6 难度曲线
- Day 1：5 单纯堂食 100% L1 并发 1；Day 2：8 单 30% 外卖；Day 3：10 单 70%L1+30%L2 解锁招牌菜；Day 4：12 单并发 3、50% 外卖、骑手提前到达事件；Day 5：12 单 50/40/10% L1/L2/L3、扣点上调 25%；Day 6：15 单并发 4、差评师；Day 7：18 单并发 5、70% 外卖、订单间隔缩短 50%。
- Week 2+：订单量每周 +20%，并发上限 6 单。

### 2.7 招牌菜系统
- 每日开店前强制设 1 个；上限 Lv.1=1 / Lv.2=2 / Lv.3=3（Max=3）。
- 效果：售价+30%、点单率+20%、堂食小费+50%、外卖补贴+5 金币；熟练度连续 3 天制作-10%、7 天-20%；高压日点单率额外 +15%，可触发"招牌菜爆单"。

### 2.8 操作与平台
- WASD/方向键移动，空格或 E 交互，一次只能手持一件物品。
- 平台路线：itch.io 首发付费验证（¥6–15）→ TapTap 免费测试 → Steam 正式发售。
- 音频：SunoAI（BGM）+ PopPop AI/freesound（音效）。

### 2.9 里程碑计划【AI 建议，12–16 周】
| Phase | 周期 | 核心交付 |
|---|---|---|
| P0 | Week 1 | 美术风格锁定 + 6 张基准素材 |
| P1 | Week 2–3 | 核心循环可运行（1 菜 + 1 堂食顾客 + 1 微波炉 + WASD + 空格交互）|
| P2 | Week 4–5 | 堂食订单 + 耐心值 |
| P3 | Week 6 | 经济结算 + 日循环 |
| P4 | Week 7–8 | 外卖 + 骑手调度 |
| P5–P9 | Week 9–16 | 设备升级、卡牌、多菜品、角色、Polish |

P1 验收标准：WASD 移动带碰撞、空格取/放/加热、手持一次一件、微波炉放包→加热 3 秒→指示灯→取出、顾客生成→入座→等待→收菜→吃 5 秒→离开、F1 调试面板、Git tag `p1`。

---

## 3. 技术细节

- 引擎：Godot 4.4.x（GDScript）【用户选型，README 中确认版本】。
- AI 生图工具：即梦（jimeng.jianying.com），模型「图片 5.0 Lite」，实际参数 1:1、2K（2048×2048）、风格化 75%、每次 4 张【用户实际操作】；提示词库文档中建议 1024×1024、风格强度 70–85%【AI】。
- 即梦无负面提示词输入框 → 排除要求融入正面提示词（`without 3d render, without realistic style, ...`）【AI 方案】。
- **关键提示词经验（2026-07-08 实测）**：`top-down perspective` 会生成正面正视图；有效结构为 `Overhead view, bird eye view from directly above, looking straight down from ceiling, [主体描述：only the top of ... visible, no face visible], [风格后缀]`。
- 统一风格后缀：`flat vector illustration, warm saturated colors, chibi proportion, clean bold outlines, 2D game asset, transparent background, single object centered, filling the frame, no text, no watermark, crisp edges, game-ready sprite, soft shadows, cozy atmosphere, consistent lighting from top-left`。
- 顾客图实为"斜上方约 45° 俯视"（可见后脑勺），非纯 90°，AI 判定游戏可用（需看背部区分身份）【AI 判断，用户接受】。
- Phase 1 代码框架（载于开发手册）【AI】：`GameStateManager.gd`（Autoload 单例）、`player_character.gd`（移动+交互+手持）、`microwave.gd`（状态机 Idle/Heating/Done）、`customer.gd`（Entering→Waiting→Eating→Leaving）、`debug_panel.gd`（F1 切换）；输入映射 WASD/空格/F1。
- 即梦官方 CLI `dreamina`：`curl -s https://jimeng.jianying.com/cli | bash` 安装，需浏览器 Cookie 认证；当时未安装，AI 建议风格测试阶段用网页版，批量生成时再装 CLI【AI 建议，用户暂缓】。
- Skill 系统：Daimon managed skills 目录 `/Users/liutongqing/Library/Application Support/kimi-desktop/daimon-share/daimon/skills/`，新对话自动扫描，无需手动引入。

---

## 4. 具体操作记录

按时间顺序（2026-07-08 凌晨）：

1. 【AI】创建 `docs/开发手册_v1.0.md`（55,549 字节：项目约定、12–16 周里程碑、P0 提示词、P1 目录/场景树/5 份 GDScript、Git 规范、20+ 条常见坑、命名速查表）。
2. 【AI】创建 `docs/进度汇报模板.md`（4,456 字节，4 种汇报模板）。
3. 【AI】创建 `docs/进度看板.md`（4,976 字节）。
4. 【AI】创建资源目录 `assets/art/{characters,effects,environment,items,ui}`、`assets/audio/{ambience,bgm,sfx}`、`assets/fonts`、`docs/prompts`；写入 `docs/prompts/AI美术提示词库_v1.0.md`（17,751 字节，约 1.8 万字）。
5. 【AI】创建 `README.md`（6,669 字节）。
6. 【AI】创建 `docs/Phase_0_执行指南.md`（12,388 字节）。
7. 【用户】即梦生成 001 批次：2 张厨师图（jimeng-2026-07-08-8816 / -9083，2048×2048）→ AI 判定为正面图非俯视，建议保留为 UI/Logo 备用 → 存为 `assets/art/characters/chef_front_view_v1.png` / `chef_front_view_v2.png`。
8. 【AI】更新提示词库至 v1.1：v1（正面立绘 UI 备用）+ v2（俯视角 sprite）双提示词，追加生成经验记录。
9. 【用户】用 v2 提示词生成 002 批次 4 张俯视厨师（jimeng-2026-07-08-7607/-8184/-4285/-9847）→ 全部真俯视；7607 透明背景被选中 → 复制为 `assets/art/characters/player_chef_idle.png`。
10. 【AI】创建 `archive/generations/001-chef_front_view/001.txt`（2,077 字节）、`002-chef_overhead_view/002.txt`（2,548 字节）；【用户】手动移图并改选中命名为 `chef_overhead_v1(selected).png`。
11. 【用户】生成 003 批次 4 张顾客（customer_overhead_v1–v4）；【AI】创建 `003-customer_overhead_view/003.txt`（1,910 字节），评估推荐 v1（暖色棕发与厨师风格统一）；【用户】选定 v1 → AI 复制为 `assets/art/characters/customer_office_waiting.png`（不带版本字样）。
12. 【AI】收工同步：进度看板更新至总体 30%（2/6 素材）、README 阶段改"🔄 进行中"。
13. 【AI】创建 `ReadmeForAgent.md`（4,978 字节）、`Lookme.html`（26,200 字节，风格对齐用户个人网页 demo，引用 `/Users/liutongqing/Downloads/跨设备同步/个人网页/index.html`）。
14. 【AI】创建 `docs/工作流规范.md`（6,507 字节）。
15. 【AI】按 `skill-creator` 规范创建通用 Skill `ai-project-companion` 于 Daimon skills 目录：`SKILL.md` + `assets/templates/readme-for-agent.md` + `assets/templates/progress-board.html` + `references/workflow-protocol.md`（首次创建因 YAML frontmatter 嵌套映射报错，修正后成功）。【用户】明确指示：创建 skills 一事**不用同步到项目文件**。
16. 【用户】询问 skills 能否获利 →【AI】诚实回答：当前无 Skill 商店/付费机制，短期不能直接变现；间接路径为开源分享、博客教程、咨询服务、个人品牌。

---

## 5. 遗留事项（对话结束时的 TODO）

- 生成 004 微波炉俯视角（提示词已给出：silver body、black glass door、control panel on right side）→ `assets/art/items/microwave_idle.png`
- 生成 005 料理包 → `assets/art/items/meal_kungpao.png`
- 生成 006 成品菜 → `assets/art/items/dish_kungpao_plated.png`
- 生成 007 地板 Tile → `assets/art/environment/floor_tile.png`
- 6 张素材齐后：导入 Godot、统一设 Filter: Nearest、测试显示
- Git commit：`p0: art style baseline`（P0 验收节点，当时尚未执行任何 git 操作）
- P0 完成后解锁 Phase 1 核心循环开发
- dreamina CLI 待装（批量生成素材时）
- `ai-project-companion` Skill 待在新对话中测试触发、边用边迭代
- Lookme.html 后续与用户个人网页内容合并
- 扩展素材（登记在提示词库状态表）：顾客学生、冰柜、炒锅(P2)、餐桌、订单气泡(P2)、耐心条(P2)、按钮(P3+)

---

## 6. 提到的文件路径与文件名清单

**当时项目根目录**：`/Users/liutongqing/Downloads/跨设备同步/游戏/爆单时刻/order_rush/`

项目文件（相对 order_rush/）：
- `README.md`、`ReadmeForAgent.md`、`Lookme.html`、`.gitignore`、（预留 `project.godot`）
- `docs/开发手册_v1.0.md`、`docs/进度汇报模板.md`、`docs/进度看板.md`、`docs/Phase_0_执行指南.md`、`docs/工作流规范.md`、`docs/ideas.md`（建议，未建）
- `docs/prompts/AI美术提示词库_v1.0.md`（已更新至 v1.1 内容）
- `archive/generations/001-chef_front_view/{001.txt, chef_front_v1.png, chef_front_v2.png}`
- `archive/generations/002-chef_overhead_view/{002.txt, chef_overhead_v1(selected).png, chef_overhead_v2.png, chef_overhead_v3.png, chef_overhead_v4.png}`
- `archive/generations/003-customer_overhead_view/{003.txt, customer_overhead_v1(selected).png, v2–v4}`
- `assets/art/characters/{player_chef_idle.png, customer_office_waiting.png, chef_front_view_v1.png, chef_front_view_v2.png}`
- 待生成目标：`assets/art/items/{microwave_idle.png, meal_kungpao.png, dish_kungpao_plated.png}`、`assets/art/environment/floor_tile.png`
- 规划文件名：`characters/customer_student.png`、`items/{fridge.png, wok.png, table_dining.png}`、`ui/{ui_order_bubble.png, ui_patience_bar_*.png, ui_button_*.png}`
- Phase 1 规划脚本：`scripts/autoload/GameStateManager.gd`、`scripts/entities/player_character.gd`、`microwave.gd`、`customer.gd`、`scripts/ui/debug_panel.gd`（载于开发手册，未创建）
- 原始生成图（Downloads）：`jimeng-2026-07-08-7607/-8184/-4285/-9847-Overhead view...png`、`jimeng-2026-07-08-8816/-9083-A chibi chef character....png`、`customer_overhead_v1–v4.png`

项目外路径：
- 个人网页 demo：`/Users/liutongqing/Downloads/跨设备同步/个人网页/index.html`
- Skill：`/Users/liutongqing/Library/Application Support/kimi-desktop/daimon-share/daimon/skills/ai-project-companion/`（SKILL.md、assets/templates/readme-for-agent.md、assets/templates/progress-board.html、references/workflow-protocol.md）
- 即梦 CLI 安装：`curl -s https://jimeng.jianying.com/cli | bash`（装到 `~/.dreamina_cli/`，程序名 `dreamina`）

---

## 附：对话第 0 轮（skill 推荐）

会话最初用户以 `kimi-skills-finder` 询问"只推荐 skills"，AI 推荐：`swarm-coding`（核心，分阶段模块化开发）、`kimi-webbridge`（下载 AI 美术资源）、`theme-factory`（统一 UI 风格）、`md-to-pdf`/`docx`（导出设计文档）；并指出当时技能库无专门 Godot/GDScript 技能。随后用户以 `swarm-coding` 重新发起同一简报，AI 判断其为规划/文档任务而非多代理并行编码任务，直接产出开发手册，未实际启用 swarm 多代理。
